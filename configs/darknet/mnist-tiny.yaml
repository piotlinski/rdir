defaults:
  - default.yaml

training:
  n_classes: 10
  data_dir: ${original_work_dir}/data/mnist
  subdivisions: 1
  pretrained: ${original_work_dir}/data/yolo/yolov4-tiny.conv.29
  cfg: ${original_work_dir}/data/yolo/yolov4-tiny.cfg.template
